# TMDB v4 read access token (Bearer). Get one from https://www.themoviedb.org/settings/api
# This is read server-side only by vite's dev proxy and never shipped to the browser.
# DO NOT prefix with VITE_ — that would expose it to the client.
TMDB_BEARER=
